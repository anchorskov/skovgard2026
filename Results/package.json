{
  "name": "skovgard-results",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "wrangler dev --persist-to ../Candidates/.wrangler/state",
    "build": "wrangler deploy --dry-run --outdir dist --env production",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
