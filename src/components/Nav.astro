---
// src/components/Nav.astro
const navLinks = [
  { href: '/about', label: 'About' },
  { href: '/civ-tech', label: 'Civ-Tech' },
  { href: '/volunteer', label: 'Volunteer' },
  { href: '/events', label: 'Events' },
  { href: '/pulse', label: 'Pulse' },
  { href: '/podcast', label: 'Podcast' },
  { href: '/finance', label: 'Finance' },
  { href: 'https://candidates.skovgard2026.org', label: 'Candidates', external: true },
  { href: '/share', label: 'Share' },
];

const current = Astro.url.pathname;
---
<nav class="fixed top-0 w-full z-50 bg-wy-charcoal/95 backdrop-blur-sm border-b-2 border-wy-ember/60">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16">
    <!-- Wordmark -->
    <a href="/" class="flex items-center gap-2">
      <span class="text-wy-bone font-serif font-extrabold text-lg leading-tight">
        Jimmy<br/><span class="text-wy-ember">Skovgard</span>
      </span>
      <span class="text-wy-bone/40 text-xs ml-2 hidden sm:block tracking-widest uppercase">U.S. Senate · 2026</span>
    </a>

    <!-- Desktop links -->
    <div class="hidden md:flex items-center gap-6">
      {navLinks.map(link => (
        <a
          href={link.href}
          {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
          class={[
            'text-sm font-medium transition-colors',
            !link.external && (current === link.href || current.startsWith(link.href + '/'))
              ? 'text-wy-ember'
              : 'text-wy-bone/80 hover:text-wy-bone'
          ].join(' ')}
        >
          {link.label}
        </a>
      ))}
    </div>

    <!-- Desktop CTA + mobile hamburger row -->
    <div class="flex items-center gap-3">
      <a
        href="/donate"
        class="bg-wy-ember text-wy-bone font-bold text-sm px-4 py-2 rounded-sm tracking-wide hover:bg-wy-ember-dark transition-colors shadow-sm"
      >
        Donate
      </a>
      <!-- Hamburger — mobile only -->
      <button
        id="nav-toggle"
        aria-label="Toggle navigation menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
        class="md:hidden text-wy-bone p-1 rounded focus:outline-none focus:ring-2 focus:ring-wy-ember"
      >
        <svg id="icon-open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="icon-close" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu (hidden by default) -->
  <div
    id="mobile-menu"
    class="hidden md:hidden bg-wy-charcoal border-t border-wy-ember/40 px-4 pb-4"
  >
    {navLinks.map(link => (
      <a
        href={link.href}
        {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
        class={[
          'block py-2 text-sm font-medium transition-colors',
          !link.external && (current === link.href || current.startsWith(link.href + '/'))
            ? 'text-wy-ember'
            : 'text-wy-bone/80 hover:text-wy-bone'
        ].join(' ')}
      >
        {link.label}
      </a>
    ))}
  </div>
</nav>

<script is:inline>
(function () {
  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');
  if (!toggle || !menu) return;
  toggle.addEventListener('click', function () {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    menu.classList.toggle('hidden');
    iconOpen.classList.toggle('hidden');
    iconClose.classList.toggle('hidden');
  });
})();
</script>
